{
  "name": "magma-copy-manager",
  "version": "0.7.0",
  "description": "Magma Copy Manager",
  "private": true,
  "scripts": {
    "release": "bash scripts/release.sh",
    "release:docker": "docker build -t magma-release-agent -f infra/Dockerfile.release . && docker run --rm -v /var/run/docker.sock:/var/run/docker.sock -v \"$(pwd):/app\" magma-release-agent",
    "clean": "npm run clean --prefix core && npm run clean --prefix app",
    "gen:api": "npm run build:openapi --prefix core && npm run gen:api --prefix app",
    "build": "npm run gen:api && npm run build --prefix core && npm run build --prefix app",
    "dev": "concurrently \"npm run dev --prefix core\" \"npm run dev --prefix app\"",
    "test": "npm run test --prefix core"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
